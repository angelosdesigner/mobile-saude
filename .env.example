# Copie para `.env.local` e ajuste os valores. `.env.local` não é versionado.
# Base da API de ocorrências (quando o backend real estiver disponível).
VITE_API_BASE_URL=http://localhost:3000

# Título exibido na aba do navegador.
VITE_APP_TITLE=Mobile Saúde
